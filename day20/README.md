 The program stores product details using arrays (ID, quantity, price). It prevents duplicate product IDs before adding a product.
You can search a product by ID and update its quantity (increase or decrease) a 3×3 rack matrix is used to find max, min, and total stock in racks.
It also calculates stock value (quantity × price) for each product and shows highest & lowest valued product.
